<local:GameWindowBase x:Class="DrugWars.Wpf.Windows.LoanSharkIntroWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DrugWars.Wpf.Windows"
        mc:Ignorable="d"
        Title="Loan Shark" Height="300" Width="500"
        WindowStartupLocation="CenterOwner"
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="NoResize"
        Icon="pack://application:,,,/Resources/images/DrugWars.ico"
        Background="{DynamicResource WindowBackground}">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextForeground}"/>
            <Setter Property="FontFamily" Value="{DynamicResource FontFamily}"/>
            <Setter Property="FontSize" Value="22"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource WindowBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForeground}"/>
            <Setter Property="BorderBrush" Value="Lime"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="20"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>
    </Window.Resources>
    <Border BorderBrush="Lime" BorderThickness="3" Margin="40" Background="#0C1F0C">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="60">
            <TextBlock Text="LOAN SHARK" FontSize="48" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,32" Foreground="{DynamicResource TextForeground}"/>
            <TextBlock x:Name="IntroTextBlock" Text="" TextAlignment="Center" Margin="0,0,0,32" FontSize="22" Foreground="{DynamicResource TextForeground}"/>
            <Button Content="OK" Width="200" Height="60" Click="OnOkClick" HorizontalAlignment="Center" Foreground="{DynamicResource TextForeground}"/>
        </StackPanel>
    </Border>
</local:GameWindowBase> 